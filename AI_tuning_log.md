# AI 回答品質調校日誌 (AI Answer Tuning Log)

本文檔記錄了對 AI 回答品質進行調校的過程、參數設定、使用者回饋以及後續的優化方案。

---

## 第一次調校 (Phase 1) - 2025-07-31

### 目標
解決 AI 回答過於冗長且不夠聚焦的問題。

### 執行變更
-   **檔案**： `ai-service/main.py`
-   **Prompt 修改**：
    -   加入嚴格規則，要求回答必須簡潔（不超過五句話）。
    -   強調必須嚴格基於提供的資料。
-   **生成參數修改**：
    -   `temperature`: `0.2`
    -   `num_predict`: `500`

### 使用者回饋
-   **優點**：反應速度變快。
-   **缺點**：回答的謹慎度和完整性變差。

---

## 第二次調校方案 (Proposed)

### 目標
在保持回應速度的同時，提升回答的完整性與品質，找到「簡潔」與「全面」之間的平衡。

### 調校方案
1.  **放寬 Prompt 長度限制**：
    -   **問題**：`"用不超過五句話簡潔地回答"` 的限制可能過於嚴格，導致模型為了遵守規則而犧牲了答案的完整性。
    -   **建議**：將提示詞修改為更具彈性的指令，例如：`"請在確保回答完整的前提下，盡可能簡潔地總結答案。"`

2.  **適度增加 `temperature`**：
    -   **問題**：`0.2` 的 `temperature` 讓回答非常固定，但可能缺乏深度和適度的變化。
    -   **建議**：將 `temperature` 適度提升至 `0.4`，允許模型在保持事實性的基礎上，生成稍微豐富一些的內容。

3.  **增加 `num_predict` (max_tokens)**：
    -   **問題**：`500` tokens 的上限可能限制了較複雜問題的回答空間。
    -   **建議**：將 `num_predict` 增加到 `1024`，為模型提供更充足的空間來生成完整答案。

---

## 第三次調校 (Phase 1.5) - 2025-08-01

### 目標
修復 RAG 無法檢索到「辦公室好習慣」相關卡片內容的問題。

### 問題診斷
通過對 RAG 系統流程分析，發現兩個關鍵問題：

1. **Category 資訊未納入向量化內容**：
   - 在 `ingest.py` 中生成卡片向量時，只包含了 `title`、`question` 和 `answer` 字段
   - 未將 `category` 字段（如「辦公室基礎好習慣」）納入向量化內容
   - 用戶使用「辦公室好習慣」查詢時無法匹配到相關卡片

2. **查詢邏輯未針對類別查詢做優化**：
   - 查詢處理未對類別關鍵詞做特殊處理
   - 向量相似度搜索無法有效處理類別概念查詢

### 執行變更

1. **修改 `ai-service/ingest.py`**：
   - 將 `category` 字段納入向量化內容：
   ```python
   content = f"{scenario['category']} {scenario['title']} {scenario['question']} {scenario['answer']}"
   ```

2. **修改 `ai-service/main.py`**：
   - 增加類別關鍵詞判斷：
   ```python
   category_keywords = ["辦公室好習慣", "基礎好習慣", "辦公室基礎好習慣", "數位檔案", "機敏資料"]
   is_category_query = any(keyword in question for keyword in category_keywords)
   ```
   - 為類別查詢增加特殊處理邏輯：
   ```python
   elif is_category_query:
       # 對於類別查詢，優先使用文本查詢加權重
       results = collection.query(
           query_texts=[question],  # 使用問題文本查詢
           n_results=3
       )
   ```

### 預期效果
- 改善系統對於類別概念查詢的響應能力
- 用戶查詢「辦公室好習慣」時能正確找到相關卡片內容
- 提升卡片檢索的全面性，不僅限於細節內容查詢
