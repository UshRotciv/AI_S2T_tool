# RAG 系統優化計劃
_版本：2.0 | 日期：2025-08-06_

## 1. 問題診斷

- **核心問題**：RAG 系統對關鍵問題（如「印表機發現無人拿走的機密文件該怎麼做？」）的檢索失敗，無法從向量資料庫中找到最相關的資訊，導致 LLM 無法給出正確答案。
- **根本原因**：目前的向量化內容（簡單字串拼接）語意表達能力不足，缺乏結構化資訊，導致嵌入模型無法精準捕捉文件語義。

## 2. 系統現況分析

- **資料來源**：`scenarios.json`, `meta_info.json`
- **當前向量化內容**：`f"{category} {title} {question} {answer}"`
- **嵌入模型**：`mxbai-embed-large`
- **向量資料庫**：ChromaDB
- **LLM**：`qwen2`

## 3. 整合式優化計劃 (Action Plan)

我們將採用循序漸進的方式，分階段實施優化，並在每個階段結束後進行嚴格測試，確保效果符合預期。

### **階段一：內容向量化與分段 (Content Vectorization & Chunking) - 高優先級**

*此階段目標是從根本上提升數據的「可檢索性」。*

- **[x] 1.1 結構化內容模板**：修改 `ingest.py`，採用結構化模板重構向量化內容，增強語義邊界。
  ```
  類別：[category]
  標題：[title]
  問題：[question]
  答案：[answer]
  學習重點：[learnings 點 1], [learnings 點 2]
  ```
- **[x] 1.2 語義分段 (Chunking)**：對於 `answer` 內容較長的資料，實施語義分段。每段（chunk）都包含完整的上下文（標題、問題等），並獨立向量化，以提高檢索粒度。
- **[x] 1.3 基準測試**：完成上述修改後，重新建立資料庫，並使用標準測試集進行第一次效能評估。

### **階段二：查詢擴增與元數據 (Query Expansion & Metadata) - 中優先級**

*此階段目標是提升系統對多樣化用戶查詢的「理解能力」。*

- **[x] 2.1 同義問法擴充**：為每個核心問題手動或使用 LLM 生成 2-3 種同義問法。這些問法將與原始問題一同納入向量化內容，以擴大檢索範圍。
  - *範例*：「看到印表機有機密文件但找不到主人怎麼辦?」
- **[x] 2.2 豐富化元數據 (Metadata)**：在 `ingest.py` 中為每筆資料（或每個 chunk）添加更豐富的元數據，如 `source`, `tags`。這將為未來的過濾查詢打下基礎。
- **[x] 2.3 第二次測試**：重新建立資料庫，並使用標準測試集評估加入同義問法後的效果。

### **階段三：混合檢索與再排序 (Hybrid Search & Re-ranking) - 中優先級**

*此階段目標是結合關鍵字與向量檢索的優點，提升複雜查詢的「魯棒性」。*

- **[x] 3.1 混合檢索**：在查詢流程中引入 BM25 等關鍵字檢索算法，與向量檢索結果進行結合。
- **[x] 3.2 結果再排序 (Re-ranking)**：使用更輕量的模型對混合檢索的初步結果進行再排序，選出最優答案。
- **[x] 3.3 第三次測試**：評估混合檢索策略對邊緣案例和複雜問題的改善效果。

#### 3.1 混合檢索實施紀錄
- 開發混合檢索腳本 `hybrid_search.py`，結合 BM25 關鍵詞檢索與向量檢索優點。
- 針對 ChromaDB 資料庫實現 BM25 索引建立與計分機制，支援任意文本問題。
- 引入加權混合機制（向量分數 ⨉ ALPHA + BM25分數 ⨉ BETA），可動態調整比重。
- 查詢結果同時包含混合分數、向量分數與 BM25 分數，方便比較分析。
- 已實作測試模式，可直接對標準測試集進行評估與比較。

#### 3.2 結果再排序實施紀錄
- 開發再排序腳本 `reranker.py`，將混合檢索的結果通過 LLM 进行高質量排序。
- 設計了專用于相關性評分的精心 prompt，包含 0-10 分的評分系統和詳細評分標準。
- 每個候選文檔都會獲得獨立評分與理由解釋，提高透明度。
- 实現了强健的 JSON 展開與解析機制，確保即使 LLM 返回格式不一致也能正確處理。
- 創建了完整的 EnhancedRAGPipeline 類，整合混合檢索與再排序為統一工作流。

#### 3.3 第三次測試實施紀錄
- 開發完整整合測試腳本 `test_enhanced_rag.py`，同時測試三種檢索方式：向量、混合與再排序。
- 定義五種測試案例，包含基礎問題、變形問題、語意挑戰問題、邊緣案例與複雜問題。
- 實現全流程測試，包含檢索、混合、再排序與最終答案生成，計算各階段耗時。
- 測試結果自動記錄於 `evaluation/test_results_phase3.json`，並提供詳細分析。
- 計算不同問題類型的平均處理時間，便於比較各階段優化的效果。

## 4. 測試與評估方法

### **標準測試集**

1.  **基礎問題**：`印表機發現無人拿走的機密文件該怎麼做?`
2.  **變形問題**：`看到印表機旁有機密文件但沒人在場該怎麼處理?`
3.  **語意挑戰問題**：`印表機旁發現一份文件，上面寫著ASUS Confidential`

### **評估指標**

- **檢索成功率 (Retrieval Success)**：是否能檢索到正確的文檔/分塊 (是/否)。
- **回答準確性 (Answer Accuracy)**：LLM 生成的回答是否準確、完整 (1-5 分)。
- **測試結果將記錄在下方進度追蹤表中。**

## 5. 實施與進度追蹤

| 日期 | 完成項目 | 測試結果 (檢索成功率 / 回答準確性) | 下一步 |
| :--- | :--- | :--- | :--- |
| 2025-08-06 | 系統分析與優化計劃 2.0 定稿 | - | 實施 `1.1 結構化內容模板` |
| | **[階段一]** | | |
| 2025-08-06 | 1.1 結構化內容模板 | 已完成 | 實施 `1.2 語義分段` |
| 2025-08-06 | 1.2 語義分段 | 已完成 | 執行 `1.3 基準測試` |
| 2025-08-06 | 1.3 基準測試 | 已完成 | 分析測試結果，進入階段二 |
| | **[階段二]** | | |
| 2025-08-06 | 2.1 同義問法擴充 | 已完成 | 實施 `2.2 豐富化元數據` |

#### 2.1 同義問法擴充優化紀錄
- 修正主程式與生成邏輯，恢復單題模式，確保每題逐條處理，流程正確。
- 增加每題 timeout（60 秒）、debug log、錯誤顯示，提升異常追蹤能力。
- 保留每 5 條自動存檔，避免中途中斷資料遺失。
- 強化資料型態檢查與資料流驗證，防止格式錯誤導致流程中斷。
- 進度條與預估剩餘時間移至 process_file 逐條顯示。
- 執行時可即時觀察每題處理狀態、LLM 回應內容與異常。
| 2025-08-06 | 2.2 豐富化元數據 | 已完成 | 執行 `2.3 第二次測試` |

#### 2.2 元數據豐富化優化紀錄
- 完成全量自動批次元數據標籤增強，涵蓋主題、風險等級、適用對象、應對時機等欄位。
- 工具自動備份原始檔案，並將增強結果寫回新檔，流程順暢。
- 每條目標籤內容可於終端即時觀察，便於人工檢查。
- 強化異常處理與資料型態驗證，確保全流程不中斷。
- 已準備好進行第二次基準測試與檢索品質評估。
| 2025-08-06 | 2.3 第二次測試 | 已完成 | 分析測試結果，進入階段三 |

#### 2.3 第二次測試實施紀錄
- 設計自動化測試腳本 `test_rag.py`，用於評估同義問法與元數據擴充成效。
- 測試涉及三種不同層次的問題：基礎對應問題、語意變形問題與語意挑戰問題。
- 定義了檢索成功率、回答準確度與響應時間等客觀評估指標。
- 在API無法連線時會自動退回至Ollama直接調用，確保測試流程穩定。
- 測試結果自動記錄於 `evaluation/test_results_phase2.json`，便於後續分析與比較。

