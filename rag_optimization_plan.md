# RAG 系統優化計畫

## 1. 根本原因分析 (Root Cause Analysis)

目前 RAG 系統在處理「印表機發現無人拿走的機密文件該怎麼做？」等關鍵問題時，出現檢索失敗，導致無法給出正確答案。其根本原因如下：

- **資料處理不當**：系統將 `app-server/scenarios.json` 這個結構化的 JSON 檔案當作單一的非結構化純文字文件進行處理。
- **語意稀釋 (Semantic Dilution)**：將整個包含多個問答對的檔案進行向量化，導致單一問題（如「印表機」）的特定語意被其他不相關的內容（如「居家辦公」、「VPN使用」）所稀釋。
- **無效的 Chunking**：由於語意被稀釋，針對特定問題的查詢向量無法在向量資料庫中找到高相關性的文件區塊 (Chunk)，從而導致檢索失敗。

## 2. 核心改善措施

為了解決此問題，我們將對資料擷取 (Ingestion) 流程進行以下核心改造：

1.  **修改資料擷取流程 (`ai-service/ingest.py`)**：
    - 讓擷取腳本能夠解析 `scenarios.json` 檔案的結構。
    - 遍歷 JSON `data` 陣列中的每一個獨立問答場景物件。

2.  **優化 Chunking 策略**：
    - 將每一個問答場景（包含 `title`, `question`, `answer`）視為一個獨立的、語意完整的 Chunk。
    - 對這個 Chunk 進行向量化，確保其在向量空間中的獨特性與高相關性。

3.  **豐富化 Metadata**：
    - 在儲存每個 Chunk 的向量時，將其對應的 `category`, `id`, `groupId` 等資訊作為 Metadata 一同存入向量資料庫。
    - 這將為未來實現更精準的過濾查詢 (Filtered Search) 或混合搜索 (Hybrid Search) 奠定基礎。

## 3. 測試與優化計畫

本計畫分為四個階段，旨在系統性地提升並驗證 RAG 系統的回答品質。

### Phase 1: 建立評估基準線 (Baseline)

**目標**: 建立一個客觀、可量化的評估標準，以衡量系統改善前後的成效。

1.  **建立黃金測試集 (Golden Set)**：
    - 建立一個名為 `evaluation/golden_set.jsonl` 的評估檔案。
    - 檔案格式為 JSON Lines，每行一個評估案例。
    - 內容應包含：
        - `question_id`: 唯一問題 ID (例如: `printer_q1`)
        - `question`: 核心問題的標準問法 (例如: `印表機發現無人拿走的機密文件該怎麼做?`)
        - `expected_answer_keywords`: 預期答案中必須包含的關鍵字列表 (例如: `["非禮勿視", "碎紙機", "通知管理師"]`)
        - `category`: 問題分類 (例如: `印表機機密文件處理`)

2.  **開發評估腳本**：
    - 建立一個 Python 腳本 `evaluation/run_evaluation.py`。
    - 該腳本讀取 `golden_set.jsonl`，向 **當前系統** 的 API 發送請求。
    - 驗證返回的答案是否包含所有 `expected_answer_keywords`。
    - 輸出評估報告，包含每個問題的通過/失敗狀態及總體通過率。

3.  **執行並記錄基準**：
    - 執行評估腳本，記錄當前系統的表現作為 Phase 1 的基準線 (預期印表機問題會失敗)。

### Phase 2: 實施核心改善

**目標**: 根據第二節的改善措施，修改程式碼並重建知識庫。

1.  **修改 `ai-service/ingest.py`**：
    - 實作前述的 JSON 解析、Chunking 及 Metadata 豐富化邏輯。

2.  **清空並重建向量資料庫**：
    - 刪除舊的 ChromaDB 資料庫。
    - 執行修改後的 `ingest.py`，將 `scenarios.json` 和其他文件以新的、正確的方式擷取至新的向量資料庫中。

### Phase 3: 評估與驗證

**目標**: 驗證 Phase 2 的修改是否成功解決了問題。

1.  **重新執行評估**：
    - 使用 Phase 1 的 `evaluation/run_evaluation.py` 腳本，對 **新系統** 進行完全相同的評估。

2.  **分析結果**：
    - 比較新舊系統的評估報告。
    - **預期結果**: 印表機相關問題的通過率應為 100%。
    - 檢查是否有其他問題因本次修改而出現退步 (Regression)。

3.  **增加測試多樣性**：
    - 在 `golden_set.jsonl` 中為核心問題增加變形問法 (Paraphrased Questions)。
    - *範例*:
        - `看到印表機旁有機密文件但沒人在場該怎麼處理?`
        - `印表機旁發現一份文件，上面寫著ASUS Confidential`
    - 重新執行評估，確保系統對語意相近的問題具有良好的泛化能力。

### Phase 4: 持續優化與監控

**目標**: 建立長期優化的迭代循環。

1.  **分析失敗案例**：
    - 針對 Phase 3 中仍然失敗的案例，深入分析是檢索 (Retrieval) 問題還是生成 (Generation) 問題。
    - **檢索問題**: 考慮引入混合搜索 (Hybrid Search)，結合關鍵字搜索 (BM25) 與向量搜索，或利用 Metadata 進行前置過濾。
    - **生成問題**: 調整 Prompt Engineering，指導 LLM 更好地利用檢索到的上下文。

2.  **整合至 CI/CD 流程**：
    - 將評估腳本整合到持續整合/持續部署 (CI/CD) 流程中。
    - 每當 `ai-service` 或知識庫內容有更新時，自動執行評估，防止系統性能退化。

3.  **建立反饋迴圈 (Feedback Loop)**：
    - 在前端應用中加入使用者反饋機制（例如「答案有幫助/無幫助」按鈕）。
    - 定期收集使用者反饋不佳的查詢，將其納入 `golden_set.jsonl`，持續擴大評估範圍，形成正向的優化循環。