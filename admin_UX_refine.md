# Admin 介面優化計畫

## 需求概述

### 初始需求
1. 保留 Create New Scenario 功能：維持創建新情境的功能完整可用
2. 卡片就地編輯：修改卡片編輯方式，由跳出編輯視窗改為直接在原卡片內編輯
3. 拖曳排序：實現卡片可拖曳重新排序功能

### 新增需求 (2025-07-30)
1. 移動 Create New Scenario 表單至頁面最下方
2. 卡片拖曳功能需與 Home 頁面顯示同步
3. 增加卡片群組功能：
   - Admin 可以將卡片拖曳進群組
   - 群組可以重新命名

## 待處理問題 (2025-07-30)
- [x] 卡片無法拖曳換位置
- [x] Add new group命名後按下group並不會產生一個可拖曳的群組框
- [x] 卡片按下edit之後所有卡片都會反白, 我只要按下edit的卡片可編輯就好
- [x] 拖曳一張卡片進去group裡會把全部一起拖曳進去
- [x] 編輯後無法儲存更新

## 問題修復紀錄 (2025-07-30)

### 當前待修復問題

#### 問題1: 拖曳卡片進群組時出現404錯誤
**錯誤訊息**: "更新失敗，已恢復原始狀態: Server responded with 404"

**根本原因分析**:
1. **後端API路由重複定義**: 
   - 第88行: `PUT /api/scenarios/:id/group` (完整實作版本)
   - 第148行: `PUT /api/scenarios/:id/group` (重複定義，簡化版本)
   - Express.js會優先使用第一個匹配的路由，但重複定義可能導致路由衝突

2. **前端拖曳邏輯**:
   - `handleDropOnGroup` 函數在第418行發送PUT請求到 `/api/scenarios/${scenarioId}/group`
   - 前端邏輯本身正確，問題出在後端路由處理

#### 問題2: 編輯卡片按下save時出現"卡片ID無效"錯誤
**錯誤訊息**: "卡片ID無效，無法更新。"

**根本原因分析**:
1. **前端ID驗證過於嚴格**:
   - 第113-117行進行嚴格的ID驗證，檢查空值、'undefined'、'null'字串
   - 可能存在ID狀態不一致的情況

2. **狀態管理問題**:
   - `editingCardId` 狀態可能與實際scenario的ID不匹配
   - 在某些情況下，ID可能被意外清空或設為無效值

3. **後端查找失敗**:
   - 當後端找不到對應ID的scenario時，返回404錯誤
   - 前端將此錯誤解讀為"卡片ID無效"

### 修復方案

#### 方案1: 修復拖曳進群組404錯誤

**後端修復** (app-server/index.js):
1. **移除重複的API路由定義**:
   - 刪除第148-159行的重複路由定義
   - 保留第88-124行的完整實作版本

2. **增強錯誤處理和日誌**:
   - 在群組更新API中加入更詳細的錯誤日誌
   - 確保API回應格式一致

**前端驗證** (AdminPage.tsx):
- 確認 `handleDropOnGroup` 函數的錯誤處理邏輯
- 驗證拖曳狀態管理的正確性

#### 方案2: 修復卡片編輯儲存錯誤

**前端修復** (AdminPage.tsx):
1. **改善ID驗證邏輯**:
   - 在 `handleCardEdit` 函數中加入ID有效性檢查
   - 確保 `editingCardId` 狀態正確設置

2. **增強錯誤處理**:
   - 在 `handleCardUpdate` 函數中加入更詳細的錯誤日誌
   - 改善錯誤訊息的用戶友好性

3. **狀態同步優化**:
   - 確保編輯狀態與scenario數據的一致性
   - 加入狀態驗證機制

**後端驗證** (app-server/index.js):
- 確認 `PUT /api/scenarios/:id` 路由的正確性
- 驗證ID查找和更新邏輯

### 實施計劃

#### 第一階段: 後端修復
1. 移除重複的API路由定義
2. 增強錯誤處理和日誌記錄
3. 測試API端點的正確性

#### 第二階段: 前端優化
1. 改善ID驗證和狀態管理
2. 增強錯誤處理和用戶反饋
3. 優化拖曳功能的穩定性

#### 第三階段: 整合測試
1. 測試拖曳卡片進群組功能
2. 測試卡片編輯和儲存功能
3. 驗證錯誤處理的正確性
   - 增加詳細日誌記錄，追蹤請求參數、找到的卡片索引等
   - 改進錯誤處理，提供更明確的錯誤訊息
   - 確保文件寫入操作有適當的錯誤捕捉
   - 增強資料處理邏輯，確保 ID 欄位不被覆蓋

## 資料儲存系統分析與優化 (2025-07-31)

### 現況分析
目前系統使用 JSON 檔案 (`scenarios.json` 和 `groups.json`) 儲存資料，我們也發現系統中存在 SQLite 資料庫檔案 (`database.db`)，但目前後端 API 並未與此資料庫互動。當前架構存在以下潛在問題：

1. **資料一致性風險**：JSON 檔案在並發存取時容易產生衝突或資料損壞
2. **擴展性限制**：隨著資料量增加，JSON 檔案讀寫效能會下降
3. **查詢能力受限**：JSON 檔案無法支援複雜查詢，所有資料操作都需在記憶體中進行
4. **資料同步困難**：如果同時有多個服務存取資料，難以保證資料同步

### SQLite 優勢評估
將資料儲存從 JSON 檔案遷移至 SQLite 資料庫有以下好處：

1. **ACID 特性**：支援交易處理，確保資料操作一致性與完整性
2. **查詢效能**：使用 SQL 進行高效查詢與過濾，不需要載入全部資料至記憶體
3. **關聯式資料處理**：更好地處理 scenarios 與 groups 之間的關係
4. **擴展性**：資料結構變更更容易實現，便於未來功能擴展
5. **RAG 整合**：可與 RAG (檢索增強生成) 系統共用同一資料庫，確保資料一致性

### 建議實施方案

#### 1. 資料庫設計
- **scenarios 表**：對應目前的 scenarios.json
  - id (主鍵)
  - category
  - title
  - question
  - answer
  - learnings (以 JSON 格式儲存陣列)
  - order
  - groupId (外鍵，關聯 groups 表)

- **groups 表**：對應目前的 groups.json
  - id (主鍵)
  - name
  - order

#### 2. 後端 API 重構
- 新增 SQLite 連接與操作邏輯
- 保持 API 端點與回應格式不變，確保前端兼容性
- 新增資料驗證層，確保資料完整性

#### 3. 資料遷移計畫
- 開發遷移腳本，將現有 JSON 資料轉換至 SQLite
- 實施雙寫機制，確保遷移過渡期資料一致性
- 保留 JSON 檔案作為備份與回滾機制

#### 4. RAG 系統整合
- 調整 RAG 系統連接至 SQLite 資料庫
- 確保編輯/更新操作自動觸發相關 embeddings 更新

#### 5. 實施時間評估
- 資料庫設計與建置：2 人天
- API 重構：3-4 人天
- 資料遷移開發與測試：2-3 人天
- RAG 系統整合：2-3 人天
- 總計：9-12 人天

## 執行清單

### 1. 準備工作
- [x] 創建 Git 分支 `20250730_recover`
- [x] 建立此執行計畫文檔
- [x] 分析現有 Admin 介面代碼結構

### 2. 保留 Create New Scenario 功能
- [x] 確認現有 Create New Scenario 功能代碼
- [x] 確保新修改不會影響此功能
- [x] 移動表單至頁面最下方
- [x] 測試功能完整性

### 3. 卡片內即時編輯功能
- [x] 分析現有卡片編輯實現方式
- [x] 設計卡片內編輯UI/UX
- [x] 實現卡片內編輯功能
  - [x] 建立編輯狀態切換機制
  - [x] 實現表單內嵌到卡片
  - [x] 加入保存/取消按鈕
  - [x] 處理表單驗證
- [x] 優化編輯體驗
  - [x] 加入編輯反饋動畫
  - [x] 實現自動保存或提示功能
- [x] 測試編輯功能

### 4. 卡片拖曳排序功能
- [x] 調研最適合的拖曳排序庫（使用HTML5原生拖曳API）
- [x] 設計拖曳排序UI/UX
- [x] 實現前端拖曳功能
  - [x] 加入視覺反饋
  - [x] 處理拖曳中狀態
  - [x] 實現放置區域提示
- [x] 實現後端順序保存機制
  - [x] 設計/更新API
  - [x] 實現順序數據持久化
- [x] 優化拖曳體驗
  - [x] 加入拖曳動畫
  - [x] 確保跨設備兼容性
- [x] 測試排序功能

### 5. 卡片拖曳與 Home 頁同步
- [x] 分析 Home 頁面顯示邏輯
- [x] 設計同步機制
- [x] 確保排序一致性
- [x] 測試同步功能

### 6. 卡片群組功能
- [x] 設計群組數據結構
  - [x] 新增 Group 型別
  - [x] 在 Scenario 中增加 groupId 關聯
- [x] 實現群組 UI
  - [x] 設計群組視覺樣式
  - [x] 實現群組展開/收起功能
- [x] 實現卡片拖曳進群組功能
  - [x] 實現群組接收區域
  - [x] 處理拖曳進群組邏輯
- [x] 實現群組命名/重命名
  - [x] 新增群組編輯功能
  - [x] 實現群組名稱保存
- [x] 群組順序與持久化
  - [x] 設計群組順序邏輯
  - [x] 實現群組順序保存機制
- [x] 測試群組相關功能

### 7. 整合與優化
- [ ] 整合所有功能確保互不衝突
- [ ] 性能優化
- [ ] 最終測試
- [ ] 確保響應式設計
- [ ] 完成最終測試

## 技術實施要點

### 卡片內編輯實現方案
- 使用狀態管理切換卡片的顯示/編輯模式
- 編輯模式下顯示表單控件替換原顯示內容
- 點擊保存時進行資料驗證和API調用

### 拖曳排序實現方案
- 使用原生 HTML5 拖曳 API 實現拖曳功能 (由於 React 版本兼容性問題未使用 react-beautiful-dnd)
- 拖曳完成後更新狀態並發送順序更新至後端
- 確保使用樂觀UI更新提供即時反饋

## 進度追蹤
- 開始日期：2025-07-30
- 計劃完成：2025-07-30
- 當前進度：完成所有功能開發
