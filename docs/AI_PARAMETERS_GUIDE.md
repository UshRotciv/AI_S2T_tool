# AI 回答參數配置指南

本文件記錄了所有會影響 AI 回答品質、長度和行為的關鍵參數設定。

## 📋 目錄
- [Ollama 模型參數](#ollama-模型參數)
- [檢索參數](#檢索參數)
- [對話歷史管理](#對話歷史管理)
- [回答完整性控制](#回答完整性控制)
- [系統提示策略](#系統提示策略)
- [參數調優建議](#參數調優建議)

---

## 🤖 Ollama 模型參數

### 核心生成參數
位置：`main.py` 第 1014-1024 行

```python
chat_response = ollama.chat(
    model=selected_model,
    messages=messages,
    options={
        'temperature': 0.1,      # 創造性控制
        'num_predict': 600,      # 最大生成長度
        'top_p': 0.8,           # 核採樣
        'top_k': 30             # 候選詞限制
    }
)
```

### 參數詳解

| 參數 | 當前值 | 作用 | 調整影響 |
|------|--------|------|----------|
| `temperature` | 0.1 | 控制回答的隨機性和創造性 | 越低越一致，越高越有創意 |
| `num_predict` | 600 | 限制生成的最大 token 數量 | 直接影響回答長度上限 |
| `top_p` | 0.8 | 核採樣，控制詞彙選擇範圍 | 影響回答的多樣性 |
| `top_k` | 30 | 限制每步選擇的候選詞數量 | 影響回答的一致性 |

### 歷史調整記錄
- **原始設定**：`num_predict: 300` → 導致回答被截斷
- **第一次調整**：`num_predict: 500` + `stop` 參數 → 回答過短
- **最終設定**：`num_predict: 600`，移除 `stop` 參數 → 達到理想平衡

---

## 🔍 檢索參數

### 分層檢索策略
位置：`main.py` 第 412-503 行 `layered_search()` 函數

```python
def layered_search(collection, query, n_results=7, timeout_sec=3.0):
```

| 參數 | 默認值 | 作用 | 調整建議 |
|------|--------|------|----------|
| `n_results` | 7 | 每層檢索返回的文檔數量 | 增加可提升覆蓋率，但會增加處理時間 |
| `timeout_sec` | 3.0 | 檢索超時時間 | 根據系統性能調整 |

### 後處理參數
位置：`main.py` 第 261-347 行 `postprocess_results()` 函數

```python
def postprocess_results(results, distance_threshold=0.45, max_per_group=2):
```

| 參數 | 默認值 | 作用 | 調整影響 |
|------|--------|------|----------|
| `distance_threshold` | 0.45 | 相似度門檻，過濾不相關結果 | 越低越嚴格，可能遺漏相關內容 |
| `max_per_group` | 2 | 每組保留的最大文檔數 | 影響回答的豐富度 |

---

## 💬 對話歷史管理

### 上下文長度控制
位置：`main.py` 第 871-883 行

```python
# 根據問題類型決定上下文長度
if is_meta_question:
    history = all_history[-2:] if all_history else []      # 元問題：1輪對話
elif any(keyword in question.lower() for keyword in ["繼續", "接著", "然後", "還有", "另外"]):
    history = all_history[-8:] if all_history else []      # 連續性問題：4輪對話
else:
    history = all_history[-5:] if all_history else []      # 一般問題：2.5輪對話
```

### 歷史記錄限制
位置：`main.py` 第 992-995 行

```python
# 只保留最近10條消息（5輪對話）
if len(conversation_history[session_id]['messages']) > 10:
    conversation_history[session_id]['messages'] = conversation_history[session_id]['messages'][-10:]
```

---

## ✅ 回答完整性控制

### 完整性檢查函數
位置：`main.py` 第 350-417 行 `ensure_complete_response()` 函數

#### 關鍵參數

| 設定 | 值 | 作用 |
|------|-----|------|
| 完整結尾標點 | `['。', '！', '？', '：', '；', '.', '!', '?', ':', ';']` | 判斷句子是否完整 |
| 截斷門檻 | 50% | 只有在完整句子位置超過文本50%時才截斷 |
| 不完整模式移除 | `'，、,'` | 移除明顯的不完整結尾 |

#### 處理邏輯
1. **完整性檢查**：檢查是否以完整標點結束
2. **截斷檢測**：識別明顯的不完整結尾
3. **智能修正**：在保留內容和完整性間平衡

---

## 📝 系統提示策略

### 核心提示
位置：`main.py` 第 815-834 行

```python
system_prompt = """你是 ASUS 資安助手，專門協助員工處理資訊安全、辦公室安全和工作流程相關問題。

**核心原則**：
1. **準確性優先**：以提供的卡片內容為主要依據
2. **智能整合**：可以整合多個卡片內容
3. **實用導向**：結合卡片內容與合理推理
4. **專業表達**：使用專業但易懂的語言
"""
```

### 動態提示增強
- **上下文提示**：根據對話歷史動態添加連貫性指引
- **檢索策略標記**：標明使用的檢索策略類型
- **特定指示**：針對特定問題類型的處理指引

---

## ⚙️ 參數調優建議

### 回答長度調整
| 需求 | 調整參數 | 建議值 |
|------|----------|--------|
| 回答太短 | `num_predict` | 增加到 700-800 |
| 回答太長 | `num_predict` | 減少到 400-500 |
| 截斷問題 | 完整性檢查門檻 | 降低到 30-40% |

### 回答品質調整
| 需求 | 調整參數 | 建議值 |
|------|----------|--------|
| 更一致的回答 | `temperature` | 降低到 0.05 |
| 更有創意的回答 | `temperature` | 提高到 0.2-0.3 |
| 更精準的檢索 | `distance_threshold` | 降低到 0.35 |
| 更豐富的內容 | `max_per_group` | 增加到 3-4 |

### 性能調整
| 需求 | 調整參數 | 建議值 |
|------|----------|--------|
| 更快的響應 | `n_results` | 減少到 5 |
| 更全面的檢索 | `n_results` | 增加到 10 |
| 減少超時 | `timeout_sec` | 增加到 5.0 |

---

## 🔧 故障排除

### 常見問題與解決方案

#### 1. 回答被截斷
**症狀**：句子說到一半就停止  
**原因**：`num_predict` 太小  
**解決**：增加 `num_predict` 值

#### 2. 回答太短
**症狀**：回答內容不夠豐富  
**原因**：`stop` 參數過早停止或完整性檢查過嚴  
**解決**：移除 `stop` 參數，調整完整性檢查門檻

#### 3. 回答不一致
**症狀**：同樣問題得到不同答案  
**原因**：`temperature` 太高  
**解決**：降低 `temperature` 到 0.1 以下

#### 4. 檢索結果不相關
**症狀**：回答內容與問題不符  
**原因**：`distance_threshold` 太寬鬆  
**解決**：降低 `distance_threshold` 值

---

## 📊 監控指標

建議監控以下指標來評估參數效果：

- **回答完整率**：完整句子結尾的比例
- **平均回答長度**：字符數統計
- **檢索相關性**：距離分數分布
- **響應時間**：從請求到回答的時間
- **用戶滿意度**：通過反饋收集

---

## 🧪 實測問題記錄與優化

### 測試情境：「設計師作品集」相關查詢
**測試日期**：2025-08-13

#### 發現的問題

**問題1：簡短查詢導致錯誤**
- **測試查詢**：「作品集」
- **結果**：連續兩次返回「Sorry, something went wrong.」
- **根因**：查詢過於簡短，檢索匹配失敗或超時

**問題2：LLM幻想內容**
- **測試查詢**：調整後的提問方式
- **結果**：獲得回答但包含非卡片內容
- **幻想內容**：提到「Google Drive 反而違反公司規定」
- **根因**：LLM 基於預訓練知識生成，未嚴格限制於卡片內容

#### 優化方案

**1. 查詢理解能力增強（修正版）**
```python
# 策略調整：增強理解能力，不限制查詢長度
context_templates = {
    "作品集": [
        "準備作品集的資安注意事項",
        "員工作品集公開規範", 
        "設計師作品集工作流程",
        "作品集避免洩露公司機密",
        "個人作品集與智慧財產權"
    ]
}

# 語義變化支持
semantic_mapping = {
    "作品集": ["作品集", "portfolio", "個人作品", "設計作品", "展示作品"]
}
```

**關鍵洞察**：
- ✅ **增強理解**：讓系統理解各種表達方式（「作品集有甚麼注意事項？」）
- ✅ **嚴格限制**：確保回答內容100%基於卡片
- ❌ **避免過度限制**：不應限制查詢的靈活性

**2. 提示詞約束強化**
```python
enhanced_constraint = """
**嚴格約束**：
1. **僅使用卡片內容**：絕對不可添加卡片中沒有的信息
2. **禁止外部知識**：不可提及 Google Drive、其他公司政策等
3. **內容驗證**：回答前檢查每個要點是否在卡片中存在
"""
```

**3. 內容驗證機制**
- 關鍵詞匹配：檢查回答中的專有名詞是否在卡片中出現
- 語義相似度：計算回答段落與卡片內容的相似度
- 規則驗證：檢查是否包含明顯的外部知識

#### 最新測試結果（2025-08-13 10:50）

**✅ 改善結果**：
- 查詢不再造成系統錯誤，能正常返回回答
- 查詢理解能力提升，支持多種表達方式

**❌ 新發現問題**：

**問題3：仍有幻想內容**
- **測試查詢**：「作品集有甚麼需要注意的？」
- **幻想內容**：提到「AWS S3」等不符合公司規定的雲端服務
- **正確服務**：公司實際使用「OneDrive」、「Teams」、「Office Outlook」
- **根因**：LLM仍在生成卡片外的技術建議

**問題4：檢索範圍過廣**
- **測試查詢**：「做作品集找工作有甚麼需要注意的？」
- **不相關內容**：檢索到「居家辦公」、「人離機鎖」等無關主題
- **根因**：情境模板擴展過於廣泛，缺乏精確性

#### 已完成的優化方案 ✅

**1. 幻想內容過濾機制**
```python
# 實作 filter_hallucinated_content() 函數
prohibited_services = {
    "AWS": "公司指定的雲端服務",
    "Amazon S3": "公司指定的雲端儲存", 
    "Google Drive": "OneDrive",
    "Dropbox": "OneDrive",
    "iCloud": "OneDrive"
}
# 整合到回答處理流程，自動檢測和替換
```

**2. 語義相關性檢查**
```python
# 實作 check_semantic_relevance() 函數
topic_groups = {
    "作品集": ["作品集", "portfolio", "設計", "創作", "展示", "求職"],
    "居家辦公": ["居家", "在家", "遠距", "remote"],
    "機鎖": ["離機", "鎖定", "螢幕", "電腦", "lock"]
}
# 過濾明顯無關的檢索結果，特別針對作品集查詢
```

**3. 精確化檢索範圍**
```python
# 優化情境模板，提高相關性
context_templates = {
    "作品集": [
        "準備作品集的資安注意事項",
        "員工作品集公開規範", 
        "未採用提案是否可放進作品集",
        "作品集避免洩露公司機密"
    ]
}
# 整合到 postprocess_results() 中
```

#### 測試驗證計劃

| 測試ID | 查詢方式 | 預期結果 | 驗證重點 |
|--------|----------|----------|----------|
| TC001 | "作品集" | 成功檢索並回答 | 簡短查詢處理 |
| TC002 | "設計師作品集注意事項" | 精確匹配卡片內容 | 具體查詢效果 |
| TC003 | "作品集可以放什麼" | 僅基於卡片內容回答 | 幻想內容檢測 |
| TC004 | "作品集上傳到哪裡" | 明確說明卡片限制 | 超範圍查詢處理 |
| TC005 | "做作品集找工作注意事項" | 只返回作品集相關內容 | 檢索精確性 |
| TC006 | 檢查回答中的雲端服務 | 不出現AWS/S3等禁用服務 | 幻想服務檢測 |

---

## 📅 更新記錄

| 日期 | 變更內容 | 影響 |
|------|----------|------|
| 2025-08-13 | 實施幻想內容過濾和語義相關性檢查 | 提升回答準確性和相關性 |
| 2025-08-13 | 創建參數配置指南 | 建立基準文檔 |
| 2025-08-13 | 修復回答截斷問題 | `num_predict`: 300→600 |
| 2025-08-13 | 優化回答長度 | 移除 `stop` 參數 |
| 2025-08-13 | 記錄實測問題與優化方案 | 發現查詢失敗和幻想內容問題 |

---
*最後更新：2025-08-13*  
*維護者：AI 系統管理員*
