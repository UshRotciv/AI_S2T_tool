# AI 回答品質提升計畫 (AI Answer Improvement Plan)

本文檔旨在提出一個分階段的行動計畫，以解決目前 AI 回答過於冗長且準確度不足的問題。計畫將依據成本效益和實施難度，從低到高分為數個階段。

---

## 問題診斷

-   **現象一：回答冗長** - AI 未能簡潔地總結資訊，可能提供了過多不必要的細節。
-   **現象二：準確度不足** - AI 可能未能檢索到最相關的知識，或是在生成答案時偏離了檢索到的內容。

---

## 優化計畫

### Phase 1: Prompt 工程與生成參數調整 (低成本，高回報)

此階段的目標是在不改動 RAG 檢索流程的前提下，透過調整對大型語言模型 (LLM) 的指令，來直接改善生成結果的品質。

**行動項目：**

1.  **精煉系統提示 (System Prompt)**：
    -   **目標**：讓 LLM 的回答風格更符合我們的期望。
    -   **作法**：在目前呼叫 LLM 的 API 請求中，加入或修改系統提示詞。例如：
        -   **長度控制**：`"請根據所提供的資料，用不超過五句話簡潔地回答。"`
        -   **準確度控制**：`"請只使用RAG資料庫內的資料來回答問題。如果資訊不足，請直接回答『根據我現有的資料，無法回答這個問題』，不要使用你自己的知識。"`

2.  **調整生成參數 (Generation Parameters)**：
    -   **目標**：降低回答的隨機性，提高事實性。
    -   **作法**：
        -   降低 `temperature` 參數（例如，從 `0.7` 降至 `0.2`），讓模型的輸出更具確定性和一致性。
        -   設定 `max_tokens` 參數，強制限制回答的總長度。

3.  **評估與迭代**：
    -   實施上述修改後，用一批標準問題來測試 AI 的回答，對比前後差異。

---

### Phase 1.5: 導入對話記憶 (Conversation Memory)

此階段的目標是讓 AI 能夠理解上下文，記住最近的對話內容，以提供更連貫的回答。

**行動項目：**

1.  **建立對話歷史機制**：
    -   **目標**：在前端或後端伺服器中，為每個使用者對話（Session）維護一個對話歷史記錄，儲存最近的（例如前 5 輪）問答配對。
    -   **作法**：
        -   **前端/App-Server**：當使用者發送新問題時，將當前問題與儲存的歷史記錄一併發送到後端 API。
        -   **API 變更**：修改 AI 服務的 API 端點，使其能夠接收一個 `history` 陣列。

2.  **將歷史記錄整合進 Prompt**：
    -   **目標**：讓 LLM 在生成回答時，能同時看到當前問題、檢索到的知識、以及先前的對話內容。
    -   **作法**：在 `ai-service` 中，重構發送給 LLM 的最終 Prompt，加入對話歷史。

3.  **設定歷史長度限制**：
    -   **目標**：控制 API 請求的 token 總量，避免成本過高或超出模型限制。
    -   **作法**：明確設定只傳遞最近的 5 輪對話歷史。

---

### Phase 2: 檢索流程優化 (中成本，顯著提升準確度)

如果 Prompt 優化後效果有限，代表問題可能出在「檢索」階段，即 AI 未能找到最相關的知識。此階段專注於改善檢索的精準度。

**行動項目：**

1.  **優化文本切分 (Chunking Strategy)**：
    -   **目標**：確保存入向量資料庫的知識片段大小適中且語意完整。
    -   **作法**：
        -   分析 `ai-service` 中目前的切分邏輯。我們是將整張卡片作為一個文件，還是將卡片的每個欄位（`title`, `question`, `answer`）分開？
        -   **實驗方案**：嘗試將一張卡片切分為更小的、圍繞單一主題的語意塊，例如將 `learnings` 中的每一點都作為獨立的知識片段存儲。

2.  **更換嵌入模型 (Embedding Model)**：
    -   **目標**：提升文本向量化的品質，讓語意相似的文本在向量空間中更接近。
    -   **作法**：評估目前使用的 Embedding 模型。如果使用的是通用模型，可以考慮更換為對中文或特定領域（如資訊安全）更友好的模型。

3.  **調整檢索數量 (Top-K)**：
    -   **目標**：在「檢索噪音」和「資訊不足」之間找到平衡。
    -   **作法**：調整 RAG 流程中檢索的文檔數量 `k`。如果 `k` 太大，可能引入不相關的資訊干擾 LLM；如果 `k` 太小，可能錯過關鍵資訊。可以從 `k=3` 或 `k=5` 開始測試。

---

### Phase 3: 源頭資料品質提升 (高成本，根本性改善)

此階段直接從知識庫 (`scenarios.json`) 本身著手，提升其品質。

**行動項目：**

1.  **審核與重寫卡片內容**：
    -   **目標**：確保每張卡片的內容都是清晰、準確、無歧義的。
    -   **作法**：人工審核所有卡片，檢查是否存在內容重疊、資訊衝突或描述模糊的問題，並進行修改。

2.  **豐富元資料 (Metadata)**：
    -   **目標**：為檢索過程提供更多可用於篩選的資訊。
    -   **作法**：在 `scenarios.json` 的每張卡片中增加更多結構化欄位，如 `keywords: ["phishing", "email"]`, `difficulty: "easy"`。在檢索時，可以先用這些元資料做一輪過濾，再進行向量相似度搜索。

---

## 建議執行步驟

1.  **從 Phase 1 開始**：首先實施 Prompt 工程與參數調整，這是最快見效的方法。
2.  **評估效果**：建立一個小的評估集（包含 5-10 個典型問題），在修改前後分別測試，記錄 AI 的回答並進行比較。
3.  **討論與決策**：根據 Phase 1 的結果，我們再一起討論是否有必要投入資源進行 Phase 2 或 Phase 3 的優化。
