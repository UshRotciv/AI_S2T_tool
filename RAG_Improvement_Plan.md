# RAG 系統改進計畫

## 1. 總結

本次診斷旨在解決 AI 服務與 RAG 向量資料庫無法正常溝通的問題。經過系統性的分析，發現問題根源並非單一原因，而是由一個致命的啟動錯誤和不穩定的路徑設定共同導致。

## 2. 問題診斷

### 2.1. 主要問題：AI 服務 (main.py) 啟動即崩潰

- **現象**: `main.py` 中使用了 `threading.Lock()` 來管理對話歷史，但檔案開頭缺少 `import threading`。
- **影響**: 這是一個 Python 語法錯誤，導致 `uvicorn` 伺服器在嘗試載入應用時立即失敗並退出。這是系統無法運作最根本的原因。

### 2.2. 次要問題：不穩定的相對路徑

- **現象**: `main.py` 和 `ingest.py` 都使用了相對路徑 (`./chroma_db`, `../app-server/scenarios.json`) 來存取資料庫和資料來源。
- **影響**: 這種寫法高度依賴腳本執行時所在的目錄，非常脆弱。如果從專案根目錄或其他位置執行，會導致找不到正確的檔案或資料庫，造成資料導入和讀取失敗。

## 3. 已執行的修復

為了徹底解決以上問題，我已對以下檔案進行了修改：

### 3.1. `ai-service/main.py`

1.  **新增模組導入**: 在檔案開頭加入了 `import threading`，解決了啟動崩潰的錯誤。
2.  **路徑穩定化**: 將 `chroma_db` 的路徑改為基於 `main.py` 檔案位置的絕對路徑。這確保了無論從何處啟動服務，都能準確找到位於 `ai-service` 目錄下的資料庫。

### 3.2. `ai-service/ingest.py`

1.  **路徑穩定化**: 同樣將 `chroma_db` 的路徑改為絕對路徑。
2.  **資料源路徑強化**: 將 `scenarios.json` 和 `meta_info.json` 的讀取路徑也改為基於 `ingest.py` 檔案位置的絕對路徑，消除了路徑依賴性。

## 4. 後續建議步驟

程式碼已修復完畢，請依照以下步驟重新啟動系統：

1.  **重新導入資料**: 手動執行一次資料導入腳本，以在正確的位置建立最新的向量資料庫。
    ```bash
    # 進入 ai-service 目錄
    cd ai-service

    # (可選，但建議) 啟動虛擬環境
    # .\venv\Scripts\activate

    # 執行導入腳本
    python ingest.py
    ```

2.  **啟動所有服務**: 回到專案根目錄，執行 `start-all.bat` 腳本來啟動所有服務。
    ```bash
    # 回到根目錄
    cd ..

    # 執行啟動腳本
    start-all.bat
    ```

完成以上步驟後，AI 服務與 RAG 資料庫之間的通訊應該已完全打通，系統可以正常運作。
